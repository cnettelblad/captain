generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Birthday {
  id     Int      @id @default(autoincrement())
  userId BigInt   @unique
  date   DateTime
}
