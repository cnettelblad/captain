generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Birthday {
  id     Int    @id @default(autoincrement())
  userId String @unique
  month  Int    // 1-12
  day    Int    // 1-31
}
